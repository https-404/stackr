# Stackr

nest project I am working on the side

----------------------------------------------------
use pnpm for it
`npm i -g pnpm`

git clone this directory

`git clone https://github.com/https-404/stackr.git`

`cd stackr`

`pnpm install`

`cp .env.example .env`

`pnpm start:dev`


--------------------------------------------------
## Warning 
This project is not stable yet,
